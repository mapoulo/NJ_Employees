spring:
  application:
    name: EMPLOYEE-SERVICE
  cloud:
    config:
      enabled: true
      uri:
      - http://localhost:8000  
  datasource:
    url: jdbc:mysql://localhost:3306/NJ_Employees_EMPLOYEES?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: NkweMapoulo@1
    username: root
  jpa:
    hibernate:
      ddl-auto: update